# Copy to .env on the host:  cp .env.example .env
#
# JVM heap per service. 8 services * 512m = ~4GB; fits the 24GB Ampere free VM.
# Lower to -Xmx384m if you also run the website + mongo on a smaller box.
JAVA_OPTS=-Xms128m -Xmx512m
